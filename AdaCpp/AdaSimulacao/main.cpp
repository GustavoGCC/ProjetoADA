#include <iostream>
#include <string>
#include <vector>

using namespace std;
/*
const vector<string> split( string frase, const char& c)
{
	string buff = "";
	vector<string> saida;

	for(int i = 0; i < frase.length() ; i++){
		if(frase[i] != c) buff += frase[i];
		else if(frase[i] == c && buff != "") {
            saida.push_back(buff);
            buff = "";
        }
	}
	if(buff != "") {
        saida.push_back(buff);
	}
	return saida;
}*/

/*int main()
{
    vector<string> saida = split("scott,tiger,", ',');
    for(int i = 0; i < saida.size() ; i++){
        cout << saida[i] << endl;
	}

}*/
